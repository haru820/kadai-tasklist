<H>Create New Task</H>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, 'Describe Task here' %>
  <%= f.text_field :content %>
  
  <%= f.submit 'Save' %>
<% end %>

<%= link_to 'Back to the List', tasks_path %>
