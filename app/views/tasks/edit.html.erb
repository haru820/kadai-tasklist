<H>Edit your Task</H>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, 'Modify Task here' %>
  <%= f.text_field :content %>
  
  <%= f.submit 'Update' %>
<% end %>

<%= link_to 'Back to the List', tasks_path %>